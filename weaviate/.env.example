# Weaviate Configuration
# Domain for Weaviate (e.g., weaviate.example.com)
# This must be a valid domain pointing to your server's public IP
WEAVIATE_DOMAIN=weaviate.example.com
WEAVIATE_VERSION=1.30.0

# Weaviate Authentication
# IMPORTANT: Generate a strong, random API key using: openssl rand -base64 32
# This key will be used by Atlas and Iris to authenticate with Weaviate
# Example format (DO NOT USE): dGhpc2lzYW5leGFtcGxlMzJieXRla2V5Zm9yd2VhdmlhdGVhcGk=
# Your key should be 44 characters long (base64 encoded 32 bytes)
WEAVIATE_API_KEY=your-secure-api-key-here
WEAVIATE_API_USER=admin

# Weaviate Logging
# Options: trace, debug, info, warning, error, fatal, panic
# Default: info (recommended for production)
WEAVIATE_LOG_LEVEL=info

# Let's Encrypt Configuration
# Email address for Let's Encrypt certificate notifications and renewal reminders
# This email will receive important notifications about certificate expiry
LETSENCRYPT_EMAIL=your-email@example.com

# IP Whitelisting (Optional)
# By default, Weaviate is accessible from anywhere with API key authentication (recommended)
# To restrict access to specific IP ranges, uncomment and set ALLOWED_IPS
#
# Examples:
#   Single IP: ALLOWED_IPS=203.0.113.1/32
#   Multiple IPs: ALLOWED_IPS=10.0.0.0/8,192.168.0.0/16,172.16.0.0/12
#   Private networks only: ALLOWED_IPS=10.0.0.0/8,192.168.0.0/16,172.16.0.0/12
#
# ALLOWED_IPS=
