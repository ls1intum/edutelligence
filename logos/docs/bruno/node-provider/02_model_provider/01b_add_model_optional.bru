meta {
  name: Add Model (Optional)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/logosdb/add_model
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "logos_key": "{{logosKey}}",
    "name": "{{modelName}}"
  }
}
