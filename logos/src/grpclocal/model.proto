syntax = "proto3";

package logos;

service LogosProxy {
  rpc Forward (ForwardRequest) returns (stream ResponseChunk);
}

message ForwardRequest {
  string url = 1;
  map<string, string> headers = 2;
  string json = 3;
}

message ResponseChunk {
  string chunk = 1;
}
