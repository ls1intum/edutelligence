meta {
  name: List Providers
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/logosdb/get_providers
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "logos_key": "{{logosKey}}"
  }
}
