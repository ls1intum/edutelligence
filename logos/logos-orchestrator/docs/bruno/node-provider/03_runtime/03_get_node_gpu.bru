meta {
  name: Get Node GPU
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/logosdb/providers/logosnode/devices
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "logos_key": "{{logosKey}}",
    "provider_id": "{{providerId}}"
  }
}
