# ============================================================================
# NEBULA LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.local and update paths for your machine.
# Then export these variables before running the services:
#   export $(cat .env.local | xargs)
# ============================================================================

# LLM Configuration (REQUIRED)
# Absolute path to the LLM config file (contains API keys, endpoints, etc.)
LLM_CONFIG_PATH=/Users/YOURNAME/projects/edutelligence/nebula/llm_config.local.yml

# Storage Configuration (REQUIRED for transcriber service)
# Absolute path for temporary video/audio files during transcription.
# This directory will be created if it doesn't exist.
NEBULA_TEMP_DIR=/Users/YOURNAME/projects/edutelligence/nebula/temp

# Log Level (OPTIONAL, default: INFO)
# LOG_LEVEL=DEBUG
