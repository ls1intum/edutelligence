# ============================================================================
# NEBULA PRODUCTION ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and update with your production values.
#
# These variables are used by Docker Compose to configure volume mounts.
# The actual container environment variables (LLM_CONFIG_PATH, NEBULA_TEMP_DIR)
# are set in docker/nebula.yml and point to the mounted paths inside containers.
# ============================================================================

# Docker Configuration
# Tag for the Docker images (latest, v1.0.0, etc.)
NEBULA_DOCKER_TAG=latest

# Configuration Files (host paths - mounted into containers)
# Path to the LLM configuration file on the host
NEBULA_LLM_CONFIG_FILE=./llm_config.production.yml

# Path to the nginx configuration file on the host
NEBULA_NGINX_CONFIG_FILE=./nginx.production.conf

# SSL Configuration
NEBULA_NGINX_SSL_CERT_FILE=/var/lib/rbg-cert/live/fullchain.pem
NEBULA_NGINX_SSL_KEY_FILE=/var/lib/rbg-cert/live/privkey.pem

# Storage Configuration (host path - mounted into container at /app/temp)
# Directory for temporary video/audio files during transcription
NEBULA_TEMP_DIR=/var/nebula/temp

# Log Level
LOG_LEVEL=INFO
