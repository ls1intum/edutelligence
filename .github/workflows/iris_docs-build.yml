name: Build Iris Documentation

on:
  workflow_call:

permissions:
  contents: read

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changes.outputs.changed }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: tj-actions/changed-files@v45
        id: changed-docs
        with:
          files: iris/docs/**

      - uses: tj-actions/changed-files@v45
        id: changed-workflow
        with:
          files: |
            .github/workflows/docs.yml
            .github/workflows/iris_docs-build.yml

      - id: changes
        run: |
          if [ "${{ steps.changed-docs.outputs.any_changed }}" = "true" ] || [ "${{ steps.changed-workflow.outputs.any_changed }}" = "true" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

  build:
    needs: detect-changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Restore Iris docs from cache
        id: cache-iris
        uses: actions/cache/restore@v5
        with:
          path: iris/docs/build
          key: iris-docs-${{ hashFiles('iris/docs/**') }}
          restore-keys: |
            iris-docs-

      - name: Setup Node.js
        if: needs.detect-changes.outputs.changed == 'true' || steps.cache-iris.outputs.cache-hit != 'true'
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: iris/docs/package-lock.json

      - name: Install dependencies
        if: needs.detect-changes.outputs.changed == 'true' || steps.cache-iris.outputs.cache-hit != 'true'
        run: |
          cd iris/docs
          npm ci

      - name: Build documentation
        if: needs.detect-changes.outputs.changed == 'true' || steps.cache-iris.outputs.cache-hit != 'true'
        run: |
          cd iris/docs
          npm run build

      - name: Save Iris docs to cache
        if: needs.detect-changes.outputs.changed == 'true' || steps.cache-iris.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: iris/docs/build
          key: iris-docs-${{ hashFiles('iris/docs/**') }}

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: iris-docs
          path: iris/docs/build
          retention-days: 1
