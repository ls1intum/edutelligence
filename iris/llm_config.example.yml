# ── Chat models (GPT-5 family) ───────────────────────────────────────────────
#
# MIGRATION NOTE: as of this commit, the name-based heuristic that used to
# decide whether to skip `temperature` for GPT-5 / o-series models has been
# removed. Each model now declares its API surface explicitly:
#   * `supports_temperature: false` — set on reasoning models (GPT-5 family,
#     o-series, etc.) that REJECT the temperature parameter. Default is
#     `true` (most chat models accept temperature).
#   * `supports_reasoning_effort: true` — set on models that accept the
#     `reasoning_effort` request parameter. Default is `false`.
#   * `supports_logprobs: true` — OPT-IN on models verified to accept the
#     `logprobs` parameter on chat completions (e.g. gpt-4o / gpt-4.1
#     families). Used by the autonomous tutor to derive confidence from
#     logprobs instead of a verbalized self-assessment. Defaults to `false`
#     everywhere: reasoning models may reject the parameter with a 400, and
#     `use_responses_api: true` models never produce logprobs on that path
#     (that combination is rejected at config load). Models without the flag
#     use the verbalized confidence strategy.
#   * `supports_top_logprobs: true` — additionally opt in the `top_logprobs`
#     parameter (top-k alternative candidates per token), which enables the
#     uncertainty scoring method (Xu et al., FSE '25); requires
#     `supports_logprobs: true`. Leave it off for strict OpenAI-compatible
#     gateways that reject unknown parameters with a 4xx; requests then keep
#     plain logprobs and confidence falls back to the mean-logprob strategy.
#     Example (verified):
#       - id: oai-gpt-4o-mini
#         type: openai_chat
#         model: gpt-4o-mini
#         supports_logprobs: true
#         supports_top_logprobs: true
- id: oai-gpt-52
  type: openai_chat
  model: gpt-5.2
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: none
  reasoning_effort_values: [none, low, medium, high, xhigh]
  cost_per_million_input_token: 3.0
  cost_per_million_output_token: 12.0
- id: oai-gpt-5-mini
  type: openai_chat
  model: gpt-5-mini
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: medium
  reasoning_effort_values: [minimal, low, medium, high]
  cost_per_million_input_token: 0.4
  cost_per_million_output_token: 1.6
- id: oai-gpt-5-mini-minimal
  type: openai_chat
  model: gpt-5-mini
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: minimal
  reasoning_effort_values: [minimal, low, medium, high]
  cost_per_million_input_token: 0.4
  cost_per_million_output_token: 1.6
- id: oai-gpt-5-nano
  type: openai_chat
  model: gpt-5-nano
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: medium
  reasoning_effort_values: [minimal, low, medium, high]
  cost_per_million_input_token: 0.1
  cost_per_million_output_token: 0.4
- id: oai-gpt-5-nano-minimal
  type: openai_chat
  model: gpt-5-nano
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: minimal
  reasoning_effort_values: [minimal, low, medium, high]
  cost_per_million_input_token: 0.1
  cost_per_million_output_token: 0.4
- id: oai-gpt-5-nano-low
  type: openai_chat
  model: gpt-5-nano
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: low
  reasoning_effort_values: [minimal, low, medium, high]
  cost_per_million_input_token: 0.1
  cost_per_million_output_token: 0.4
- id: oai-gpt-5.5
  type: openai_chat
  model: gpt-5.5
  api_key:
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: medium
  reasoning_effort_values: [none, low, medium, high, xhigh]
  # GPT-5.5 rejects reasoning_effort+tools on chat completions; /responses is supported.
  use_responses_api: true
  cost_per_million_input_token: 5.00
  cost_per_million_output_token: 30.00

# ── Azure example (GPT 5 Mini) ──────────────────────────────────────────────
- id: azure-gpt-5-mini
  type: azure_chat
  model: gpt-5-mini
  api_key:
  api_version: 2025-04-01-preview
  azure_deployment: gpt-5-mini
  endpoint: "<your-endpoint>"
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: medium
  reasoning_effort_values: [minimal, low, medium, high]
  cost_per_million_input_token: 0.4
  cost_per_million_output_token: 1.6

# ── Azure example (GPT 5.5 — supports reasoning effort) ─────────────────────
- id: azure-gpt-5.5
  type: azure_chat
  model: gpt-5.5
  api_key:
  api_version: 2025-04-01-preview
  azure_deployment: gpt-5.5
  endpoint: "<your-endpoint>"
  supports_temperature: false
  supports_reasoning_effort: true
  reasoning_effort: medium
  reasoning_effort_values: [none, low, medium, high, xhigh]
  # GPT-5.5 rejects reasoning_effort+tools on chat completions; /responses is supported.
  use_responses_api: true

# ── Embedding models ─────────────────────────────────────────────────────────
- id: azure-embedding-large
  type: azure_embedding
  model: text-embedding-3-large
  api_key:
  api_version: "2023-05-15"
  azure_deployment: te-3-large
  endpoint: "<your-endpoint>"
  cost_per_million_input_token: 0.13
- id: oai-embedding-small
  type: openai_embedding
  model: text-embedding-3-small
  api_key:
  cost_per_million_input_token: 0.02

# ── Whisper transcription models ─────────────────────────────────────────────
- api_key:
  description: Whisper 1 (OpenAI)
  id: whisper-1-openai
  model: whisper-1
  name: Whisper 1
  type: openai_whisper
- api_key:
  api_version: "2024-06-01"
  azure_deployment: whisper
  description: Whisper 1 on Azure
  endpoint: "<your-endpoint>"
  id: whisper-1-azure
  model: whisper-1
  name: Whisper 1 (Azure)
  type: azure_whisper

# ── Reranker ─────────────────────────────────────────────────────────────────
- id: cohere
  type: cohere_azure
  model: "rerank-multilingual-v3.5"
  api_key:
  endpoint: "your_cohere-endpoint"
  cost_per_1k_requests: 2

# ── Ollama models (local) ───────────────────────────────────────────────────
# TUM-internal users: substitute your own Ollama/OpenAI-compatible endpoint (e.g. https://gpu.aet.cit.tum.de/ollama)
- id: gemma3
  type: ollama
  model: gemma3:27b
  host: https://your-ollama-or-openai-compatible-endpoint
  api_key: ""
  cost_per_million_input_token: 0
  cost_per_million_output_token: 0
- id: gpt-oss
  type: ollama
  model: gpt-oss:120b
  host: https://your-ollama-or-openai-compatible-endpoint
  api_key: ""
  cost_per_million_input_token: 0
  cost_per_million_output_token: 0
# Logos/vLLM-served gpt-oss accepts ONLY low|medium|high for reasoning_effort;
# any other value is a hard 400. For an OpenAI-compatible gpt-oss entry, use:
# type: openai_chat, base_url: <logos-url>, supports_reasoning_effort: true,
# reasoning_effort: medium, reasoning_effort_values: [low, medium, high].
- id: mxbai-embed-large
  type: ollama
  model: mxbai-embed-large:latest
  host: https://your-ollama-or-openai-compatible-endpoint
  api_key: ""
  cost_per_million_input_token: 0
  cost_per_million_output_token: 0
- id: nomic-embed-text
  type: ollama
  model: nomic-embed-text:latest
  host: https://your-ollama-or-openai-compatible-endpoint
  api_key: ""
  cost_per_million_input_token: 0
  cost_per_million_output_token: 0

# ── Currently only for integration tests (not part of normal test suite) of Ask-user mode pipeline ─────────────────────────────────────────────────────────────────
- id: gemini-3.1-pro
  type: google_gemini
  model: gemini-3.1-pro-preview
  api_key: ""
  supports_temperature: false
  generation_config:
    thinkingConfig:
      thinkingLevel: medium
  cost_per_million_input_token: 2.0
  cost_per_million_output_token: 12.0